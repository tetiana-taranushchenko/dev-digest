# client/specs/

Feature specs written before building something in `client/` — e.g. a new
page, a new hook, a UI flow change. One file per feature.

A spec whose acceptance criteria or contracts require coordinated changes
across `client/` and `server/` and/or `reviewer-core/` belongs in
[`specs/`](../../specs/README.md) instead.
