import { ContextView } from "./_components/ContextView";

/* Route: /context (Project Context, G-1). Thin route entry — the view, its
   styles, helpers, and row component are colocated under _components/ContextView. */
export default function ContextPage() {
  return <ContextView />;
}
