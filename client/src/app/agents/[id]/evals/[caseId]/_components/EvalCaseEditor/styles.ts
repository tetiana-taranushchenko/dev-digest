import type { CSSProperties } from "react";

/** Co-located styles for EvalCaseEditor (extracted from inline styles). */
export const s = {
  root: { display: "flex", flexDirection: "column", height: "100%", minHeight: 480 } satisfies CSSProperties,
  topbar: {
    padding: "16px 18px",
    borderBottom: "1px solid var(--border)",
    display: "flex",
    alignItems: "center",
    gap: 12,
  } satisfies CSSProperties,
  topIcon: { color: "var(--accent)" } satisfies CSSProperties,
  topTitle: { fontSize: 15, fontWeight: 600 } satisfies CSSProperties,
  topActions: { marginLeft: "auto", display: "flex", gap: 10 } satisfies CSSProperties,
  grid: { display: "grid", gridTemplateColumns: "1fr 1fr", flex: 1, minHeight: 0 } satisfies CSSProperties,
  left: {
    borderRight: "1px solid var(--border)",
    display: "flex",
    flexDirection: "column",
    minWidth: 0,
  } satisfies CSSProperties,
  nameField: { padding: "16px 18px 0" } satisfies CSSProperties,
  inputLabelWrap: { padding: "0 18px" } satisfies CSSProperties,
  inputLabel: {
    fontSize: 13,
    fontWeight: 600,
    color: "var(--text-secondary)",
    marginBottom: 8,
  } satisfies CSSProperties,
  tabBody: { flex: 1, overflow: "auto", padding: "14px 18px" } satisfies CSSProperties,
  diffArea: {
    width: "100%",
    minHeight: 280,
    resize: "vertical",
    fontFamily: "var(--font-mono, monospace)",
    fontSize: 12,
    lineHeight: 1.6,
    background: "var(--code-bg)",
    color: "var(--text-primary)",
    border: "1px solid var(--border)",
    borderRadius: 7,
    padding: 12,
  } satisfies CSSProperties,
  previewWrap: { marginTop: 14 } satisfies CSSProperties,
  previewLabel: { fontSize: 12, color: "var(--text-muted)", marginBottom: 8 } satisfies CSSProperties,
  right: { display: "flex", flexDirection: "column", minWidth: 0 } satisfies CSSProperties,
  expectedHead: {
    padding: "16px 18px 10px",
    display: "flex",
    alignItems: "center",
    gap: 10,
  } satisfies CSSProperties,
  expectedLabel: { fontSize: 13, fontWeight: 600, color: "var(--text-secondary)" } satisfies CSSProperties,
  expectedArea: {
    margin: "0 18px",
    flex: 1,
    minHeight: 220,
    fontFamily: "var(--font-mono, monospace)",
    fontSize: 12,
    lineHeight: 1.55,
    background: "var(--code-bg)",
    color: "var(--text-primary)",
    border: "1px solid var(--border)",
    borderRadius: 7,
    padding: 14,
    resize: "vertical",
  } satisfies CSSProperties,
  result: (pass: boolean): CSSProperties => ({
    margin: "14px 18px 18px",
    padding: "12px 14px",
    borderRadius: 8,
    border: `1px solid ${pass ? "rgba(16,185,129,0.3)" : "var(--crit)"}`,
    background: pass ? "var(--ok-bg)" : "var(--crit-bg)",
    display: "flex",
    alignItems: "center",
    gap: 10,
  }),
  resultIcon: (pass: boolean): CSSProperties => ({ color: pass ? "var(--ok)" : "var(--crit)" }),
  resultText: { fontSize: 13, color: "var(--text-secondary)" } satisfies CSSProperties,
  resultStrong: { color: "var(--text-primary)" } satisfies CSSProperties,
} as const;
