import type { CSSProperties } from "react";

/** Co-located styles for EvalDashboard (extracted from inline styles). */
export const s = {
  loading: { padding: 32, color: "var(--text-muted)", fontSize: 14 } satisfies CSSProperties,
  page: (compact?: boolean): CSSProperties => ({
    padding: compact ? "0" : "20px 28px 40px",
    maxWidth: 980,
    margin: "0 auto",
  }),
  header: { display: "flex", alignItems: "flex-end", marginBottom: 20 } satisfies CSSProperties,
  h1: { fontSize: 24, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  subtitle: { fontSize: 14, color: "var(--text-secondary)", marginTop: 4 } satisfies CSSProperties,
  headerActions: { marginLeft: "auto", display: "flex", gap: 10, alignItems: "center" } satisfies CSSProperties,
  alert: {
    display: "flex",
    gap: 12,
    alignItems: "center",
    padding: "12px 16px",
    borderRadius: 8,
    border: "1px solid var(--warn)",
    background: "var(--warn-bg)",
    marginBottom: 20,
  } satisfies CSSProperties,
  alertIcon: { color: "var(--warn)" } satisfies CSSProperties,
  alertText: { fontSize: 14, color: "var(--text-secondary)" } satisfies CSSProperties,
  metricsRow: { display: "flex", gap: 16, marginBottom: 24 } satisfies CSSProperties,
  trendCard: { marginBottom: 24 } satisfies CSSProperties,
  trendHead: { display: "flex", alignItems: "center", gap: 18, marginBottom: 14 } satisfies CSSProperties,
  legend: { marginLeft: "auto", display: "flex", gap: 16, fontSize: 12 } satisfies CSSProperties,
  legendItem: { display: "inline-flex", alignItems: "center", gap: 6, color: "var(--text-secondary)" } satisfies CSSProperties,
  legendSwatch: (color: string): CSSProperties => ({ width: 10, height: 2, background: color, borderRadius: 2 }),
  tableWrap: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    overflow: "hidden",
    background: "var(--bg-elevated)",
  } satisfies CSSProperties,
  headRow: (cols: string): CSSProperties => ({
    display: "grid",
    gridTemplateColumns: cols,
    gap: 14,
    padding: "10px 18px",
    background: "var(--bg-surface)",
    borderBottom: "1px solid var(--border)",
    fontSize: 12,
    fontWeight: 700,
    letterSpacing: "0.05em",
    color: "var(--text-muted)",
    textTransform: "uppercase",
  }),
  emptyRuns: { padding: 20, fontSize: 13, color: "var(--text-muted)" } satisfies CSSProperties,
  row: (cols: string, last: boolean): CSSProperties => ({
    display: "grid",
    gridTemplateColumns: cols,
    gap: 14,
    padding: "12px 18px",
    borderBottom: last ? "none" : "1px solid var(--border)",
    alignItems: "center",
    fontSize: 13,
  }),
  ranAt: { color: "var(--text-secondary)", fontSize: 12 } satisfies CSSProperties,
  passCell: (pass: boolean | null): CSSProperties => ({
    fontWeight: 600,
    color: pass ? "var(--ok)" : "var(--crit)",
  }),
  costCell: { color: "var(--text-secondary)" } satisfies CSSProperties,
} as const;
