/** Constants for the Settings → Plugins & Digest section. */

/** Default name used when exporting the workspace as a plugin bundle. */
export const EXPORT_NAME = "devdigest-export";

/** Filename for the downloaded plugin bundle. */
export const EXPORT_FILENAME = "devdigest-plugin.json";

/** MIME type for the exported bundle blob / file input. */
export const JSON_MIME = "application/json";

/** Rows for the import textarea. */
export const IMPORT_TEXTAREA_ROWS = 5;
