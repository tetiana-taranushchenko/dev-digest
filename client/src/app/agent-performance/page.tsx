import { AgentPerformanceView } from "./_components/AgentPerformanceView";

/* Route: /agent-performance (?sort). Thin route entry — the view, its styles,
   constants, helpers and test are colocated under _components/AgentPerformanceView. */
export default function AgentPerformancePage() {
  return <AgentPerformanceView />;
}
