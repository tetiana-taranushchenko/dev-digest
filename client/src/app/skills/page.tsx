import { SkillsListView } from "./_components/SkillsListView";

/* Route: /skills (Skills Lab). Thin route entry — the view, its styles,
   constants, helpers and i18n are colocated under _components/SkillsListView. */
export default function SkillsPage() {
  return <SkillsListView />;
}
