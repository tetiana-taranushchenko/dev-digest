/* context-picker — shared attach UI for Project Context documents (T12).
   Public surface: the ContextDocPicker component and its prop/row types. */
export { ContextDocPicker } from "./ContextDocPicker";
export type { ContextDocPickerProps, ContextDocRow } from "./types";
export { CONTEXT_TOKEN_CAP_FALLBACK } from "./constants";
