/** Constants for the plugins module. */

/** Bundle format identifier emitted/accepted by export/import. */
export const PLUGIN_FORMAT = 'devdigest-plugin/v1';

/** Default bundle name/version when not supplied by the caller. */
export const DEFAULT_BUNDLE_NAME = 'workspace-plugin';
export const DEFAULT_BUNDLE_VERSION = '1.0.0';

/** Source recorded for plugins created via import. */
export const IMPORT_SOURCE = 'import';
