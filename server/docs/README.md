# server/docs/

Deeper design notes on `server/` that don't fit `README.md`'s map (e.g. a
walkthrough of one non-obvious subsystem). `README.md` stays the single
source of truth for the request/DI-flow and API-map diagrams — link to it,
don't restate it here.
