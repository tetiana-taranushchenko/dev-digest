# server/specs/

Feature specs written before building something in `server/` — e.g. a new
module, a route contract change, a schema addition. One file per feature.

A spec whose acceptance criteria or contracts require coordinated changes
across `server/` and `client/` and/or `reviewer-core/` belongs in
[`specs/`](../../specs/README.md) instead.
