# mcp-server/specs/

Feature specs written before building something in `mcp-server/` — e.g. a
new tool, a changed request/response contract. One file per feature.

A spec whose acceptance criteria or contracts require coordinated changes
across `mcp-server/` and `server/` and/or other modules belongs in
[`specs/`](../../specs/README.md) instead.
