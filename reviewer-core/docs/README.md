# reviewer-core/docs/

Deeper design notes on `reviewer-core/` that don't fit `README.md`'s map
(e.g. a walkthrough of one non-obvious subsystem). `README.md` stays the
single source of truth for the pipeline diagram — link to it, don't restate
it here.
