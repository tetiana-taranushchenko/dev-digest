# reviewer-core/specs/

Feature specs written before building something in `reviewer-core/` — e.g. a
new prompt slot, a change to the grounding gate. One file per feature.

A spec whose acceptance criteria or contracts require coordinated changes
across `reviewer-core/` and `server/` and/or `client/` belongs in
[`specs/`](../../specs/README.md) instead.
