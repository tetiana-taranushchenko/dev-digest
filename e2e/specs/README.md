# e2e/specs/

The flow definitions themselves — not feature specs. Each `NN-name.flow.json`
is an ordered list of `agent-browser` commands plus deterministic assertions.
See [`../README.md`](../README.md) for how a flow works and the full coverage table.
