{
  "name": "PR Overview shows the seeded intent summary, scope lists, and confidence badge",
  "description": "From PR #482, stay on the default Overview tab and confirm the INTENT panel renders the seeded pr_intent row (server/src/db/seed.ts) — summary quote, both scope lists, and the MEDIUM confidence badge — without triggering a real classifier call. Exercises IntentPanel / ScopeList / ConfidenceBadge. Read-only seeded data (no model call). Assumes a freshly-seeded DB where acme/payments-api (PR #482) is the first repo — guaranteed by e2e-web.yml; run locally via the hermetic stack (see e2e/README.md).",
  "steps": [
    { "cmd": ["open", "{BASE}/"], "label": "load the app root" },
    { "cmd": ["wait", "--url", "/pulls"], "label": "land on the PR list" },
    { "cmd": ["wait", "--text", "Add rate limiting to public API endpoints"], "label": "seeded PR row is visible" },
    { "cmd": ["find", "text", "Add rate limiting to public API endpoints", "click"], "label": "open the PR row" },
    { "cmd": ["wait", "--url", "/pulls/482"], "label": "navigate to the PR detail route" },
    { "cmd": ["wait", "--load", "networkidle"], "label": "PR detail data settles" },
    { "cmd": ["wait", "--text", "INTENT"], "label": "INTENT panel renders on the default Overview tab" },
    { "cmd": ["wait", "--text", "Add token-bucket rate limiting to public API endpoints"], "label": "seeded intent summary is visible" },
    { "cmd": ["wait", "--text", "Rate-limit middleware for public endpoints"], "label": "seeded in-scope item is visible" },
    { "cmd": ["wait", "--text", "Distributed/multi-instance rate limiting"], "label": "seeded out-of-scope item is visible" },
    { "cmd": ["wait", "--text", "MEDIUM"], "label": "seeded confidence badge renders its tier label" }
  ]
}
