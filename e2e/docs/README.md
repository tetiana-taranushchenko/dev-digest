# e2e/docs/

Deeper design notes on `e2e/` that don't fit `README.md`'s map (e.g. a
walkthrough of the hermetic runner or agent-browser setup). `README.md`
stays the single source of truth for the coverage table — link to it, don't
restate it here.
